import React from 'react';

import { useStateContext } from '../contexts/ContextProvider';

const Cart = () => {
  <a href="src/components/model.docx" download></a>
  const { currentColor } = useStateContext();

  return (
   <div> 
    <a href="src/components/model.docx" download></a>
   </div>
  );
};

export default Cart;
